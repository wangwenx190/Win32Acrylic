# TODO

- 支持使用命令行参数设置自定义的窗口类名、窗口标题、窗口几何尺寸与位置
- 声明高对比主题下的调色板
- 将WindowsVersion重命名为VersionNumber，并添加两个函数：VersionNumber fromString(LPCWSTR)与LPCWSTR toString()
- 用XAML控件自制一个标题栏，并在WM_NCHITTEST里正确的返回三个系统按钮的位置
- 程序运行时输出当前Windows版本，当前DPI，当前DPI感知级别等必要信息
- 将AcrylicApplication整合为一个类
