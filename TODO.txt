# TODO

- 支持使用命令行参数设置自定义的窗口类名、窗口标题、窗口几何尺寸与位置
- 声明高对比主题下的调色板
- 用XAML控件自制一个标题栏，并在WM_NCHITTEST里正确的返回三个系统按钮的位置
- 将AcrylicApplication整合为一个类
- 点击窗口左上角窗口图标位置，弹出系统菜单
- 缓存窗口状态、系统主题等，不要每次都全新查询
- 看能否将SystemLibraryManager的宏改进为类或函数
- 添加将系统版本号转换为人类可读版本信息的函数，并在合适的位置输出（程序启动时？）
- 添加将DPI感知级别转换为人类可读信息的函数，并在合适的位置输出（程序启动时？）
- DWMAPI_API -> DWM_API
- 写一个错误包装类，可以直接从win32 error code以及hresult构造，如果返回值不是success则自动获取错误信息
- VC-LTL 设置为可选项
- 添加clang-cl、intel-cl以及mingw-w64的工具链
